# Provisioned automatically when a Vercel Blob store is connected to this project
# (Vercel dashboard -> Storage -> Blob). For local dev, pull it down with:
#   vercel env pull .env.development.local
BLOB_READ_WRITE_TOKEN=
